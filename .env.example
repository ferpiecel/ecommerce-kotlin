# ============================================
# ShopNow Backend - Environment Variables
# ============================================
# Copy this file to .env and adjust values as needed:
# cp .env.example .env
#
# NOTE: When using Docker Compose, most of these variables
# are automatically configured. You only need to customize
# if you want different values.

# ============================================
# Database Configuration (PostgreSQL)
# ============================================
DATABASE_NAME=shopnow
DATABASE_USER=shopnow
DATABASE_PASS=shopnow
DATABASE_PORT=5432

# ============================================
# Application Configuration
# ============================================
PORT=8080

# Spring Profile (use 'docker' for Docker, 'dev' for local development)
SPRING_PROFILES_ACTIVE=docker

# ============================================
# Database Connection (for local development)
# ============================================
# When running with Docker Compose, these are set automatically
# Only needed when running the app locally (./gradlew bootRun)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=shopnow
DB_USER=shopnow
DB_PASSWORD=shopnow

# ============================================
# Redis Configuration
# ============================================
# When using Docker Compose, host will be 'redis' (service name)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=

# ============================================
# Security (JWT)
# ============================================
JWT_SECRET=change-this-secret-in-production-use-a-long-random-string
JWT_EXPIRATION=86400000

# ============================================
# CORS Configuration
# ============================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:4200

# ============================================
# Development Tools
# ============================================
# pgAdmin Web UI
PGADMIN_EMAIL=admin@shopnow.local
PGADMIN_PASSWORD=admin
PGADMIN_PORT=5050

# ============================================
# Optional: Logging Configuration
# ============================================
# LOGGING_LEVEL_ROOT=INFO
# LOGGING_LEVEL_COM_SHOPNOW=DEBUG
# LOGGING_LEVEL_ORG_SPRINGFRAMEWORK_R2DBC=DEBUG
