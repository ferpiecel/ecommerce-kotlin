package com.shopnow.shared.kernel.domain

/**
 * Value Object marker interface
 *
 * Value Objects are immutable objects defined by their attributes rather than identity.
 * They should be compared by value, not by reference.
 */
interface ValueObject
